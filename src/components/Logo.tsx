import React from "react";

const Logo: React.FC = () => {
  return (
    <h1 className="logo-heading">
      Op<span style={{ color: "#28A745" }}>Ready</span>
    </h1>
  );
};

export default Logo;
